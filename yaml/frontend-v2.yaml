apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-v2
  namespace: default
  labels:
    app: frontend-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-v2
  template:
    metadata:
      labels:
        app: frontend-v2
    spec:
      containers:
        - name: frontend-v2
          image: linsun/vision-frontend:kcdperu
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: BACKEND_HOST
              value: "backend"
            - name: BACKEND_PORT
              value: "8080"
          resources:
            requests:
              cpu: "20m"
              memory: "32Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-v2
  namespace: default
spec:
  selector:
    app: frontend-v2
  ports:
    - port: 80
      targetPort: 80
